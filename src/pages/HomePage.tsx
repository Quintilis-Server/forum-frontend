import {BasePage} from "./BasePage.tsx";
import * as React from "react";

export class HomePage extends BasePage<any, any>{
    protected renderContent(): React.ReactNode {
        return (
            <main>

            </main>
        );
    }
}